{
  "name": "@nodi/infra",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "cdk": "cdk"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
