{
  "name": "@nodi/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"@nodi/web: not implemented yet (S1-4)\"",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
