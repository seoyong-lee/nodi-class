---
description: AWS CDK infra rules (PLAN.md §12)
globs: infra/**
---

- 단일 스택: `NodiClassStack` (`stackName: nodi-class`).
- AWS 리소스 이름 prefix: `nodi-class-`.
- 테이블 `RemovalPolicy.RETAIN` (단일 prod-like 환경).
- 시크릿은 코드에 절대 없음. SSM SecureString 참조만 (`/nodi-class/…`).
